import { Routes } from '@angular/router';
import { LandingPage } from './commercial/pages/landing-page/landing-page';

export const routes: Routes = [
    {
        path: '', component: LandingPage
    }
];
